defun factorial(n) 
{
    if n == 1 {
        return 1;
    }
    return factorial(n-1) * n;
    z = 10;
}

defun main()
{ 
    x = 0 * (1 + 2);
    y = 2^3;
    in x;
    res = factorial(x);
    out res;
    return res;
}
